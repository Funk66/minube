{
	email admin@guirao.net
	admin :2019
	metrics {
		per_host
	}
	log {
		output file /var/log/caddy/server.log
	}
}

(proxy) {
	reverse_proxy {args[0]}:{args[1]}
	log {
		format transform "{common_log}"
		output file /var/log/caddy/{args[0]}.log
	}

	@blockedPaths {
		path */.git*
		path */.env*
		path */*.php
		path */db.sqlite3*
		path */.htaccess*
	}
	abort @blockedPaths
}

photos.guirao.net {
	import proxy immich 2283
}

calendar.guirao.net {
	import proxy radicale 5232
}

mail.guirao.net {
	import proxy stalwart 8080
}

http://, https:// {
	abort
}
